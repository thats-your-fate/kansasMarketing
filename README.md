# Future Wells Kansas — Marketing

Next.js 14 App Router SEO site for Kansas oil & gas well data, following the same architecture as the sibling Future Wells state marketing sites (Wyoming, Oklahoma, Colorado). Paired with [`kansasBackend`](https://github.com/thats-your-fate/kansasBackend).

See `future_wells_kansas_backend_marketing_codex_tasks.md` (in the backend repo) for the full plan, and `docs/architecture/kansas-marketing-boundary.md` for fixed module boundaries.

## Local development

```bash
cp .env.example .env   # BACKEND_API_URL defaults to http://127.0.0.1:4008
npm install
npm run dev             # serves on :3009
```

Requires `kansasBackend`'s API running locally (or `BACKEND_API_URL` pointed at a reachable instance) for the landing page's backend-status card to show "ok".

## Changelog

- **KS-M001** (2026-09-04): Scaffolded the project — Next.js 14 App Router project, `site-config.mjs` (stateCode `KS`, both KCC and KGS credited as separate agencies), `app/config.ts` with the indexing-safety guard, root layout, a `/kansas` landing page that server-fetches the backend `/health` endpoint, placeholder `robots.txt`/`sitemap.xml` routes, deploy script + systemd unit.
- **KS-M001 addendum** (2026-09-05): Following an architecture correction (backend task doc §10: this site must never build public well search, a map, or browse-first directories — that belongs to the shared `futurewells.co/user-area` product), replaced the landing page's "under construction" framing with a real conversion path. Ported `components/future/RecordConversionCta.tsx` and `config/userArea.ts` verbatim from `coloradoMarketing`/`wyomingMarketing` (the same shared pattern both sibling sites already use to funnel visitors to `futurewells.co/user-area/register`), added a minimal `app/analytics.ts` (just the two event names the CTA needs), and added a CTA card on `/kansas` linking to account registration instead of promising a self-hosted search/map. No entity pages (wells/operators/production/etc.) are planned — see the updated marketing task queue (task doc §9) and `docs/architecture/kansas-marketing-boundary.md`.
- **Baseline commit** (2026-09-06): The KS-M001 work above had never actually been committed (this repo's git history was empty despite the changelog describing it as done) — committed as a single baseline commit before KS-18/KS-19 landed on top, so both land as reviewable diffs.
- **KS-18** (2026-09-06): Cloned `coloradoMarketing`'s design system and page structure with Kansas content — new `app/future.css` (design tokens, nav/mobile-nav, hero, cards, grids, sections, footer, guide/copy blocks — ported from Colorado's `app/future.css`, trimmed of the vendor Bootstrap theme and legacy `aos`/`gsap`/`swiper` dependencies Colorado's full data-app pages need but this editorial-only site does not), a real `components/future/MarketingLayout.tsx` (header/nav/mobile-nav/footer/`ProductCta`/`DisclaimerStrip`, Kansas nav trimmed to Data/Activity/Guides/Methodology per the ticket since this site never builds a public map or browse-first directories), and a Kansas-shaped `components/future/HeroMapPreview.tsx` (a pure CSS/React illustration, no map library — same technique Colorado's own hero graphic uses, a hand-tuned `clip-path` polygon rather than a real boundary file). **Moved all real content to top-level routes** (`/`, `/data`, `/data-coverage`, `/methodology`, `/activity`, `/guides`, `/disclaimer`, `/privacy`, `/terms`, `/contact`), superseding KS-M001's original `/kansas`-prefixed convention — Colorado's own marketing pages are top-level (only its full data-app pages sit under `/colorado/*`), and the ticket's own route list is written top-level; `/kansas` now redirects to `/`. New `config/userArea.ts::futureWellsCtaHref(source)` — every product CTA now carries `state=KS` plus a source tag (Colorado's identical constant carries no query params at all; this is new work beyond what was ported, required by the ticket's own acceptance criteria). Deliberately **not** ported: Colorado's live-backend `StartBrowseSections`/summary-list homepage sections (needs wells/operators/counties data this site doesn't have), the interactive `ContactInquiryForm` (posts to a Colorado-only `/api/public/inquiries` backend endpoint with Turnstile captcha verification that has no Kansas equivalent — `/contact` uses a plain `mailto:` instead), and Colorado's route-prefix SEO-policy allowlist (`app/seo-policy.ts` — unnecessary here since every route this repo builds is an intentional, approved page, not a mix of thin/deep-pagination pages needing per-route gating). `/data-coverage` also closes a deferred item from `kansasBackend`'s own KS-17 task (its `docs/ops/ks-17-monitoring-handoff.md` explicitly asked for "a simplified public coverage page") — its per-source cadence table is the real `expected_cadence` values that repo tracks, not invented for this page. Automated visual-regression tooling (the ticket's own acceptance criterion) was explicitly scoped out of this pass — neither this repo nor `coloradoMarketing` has any screenshot-diff tooling installed; verified instead by comparing rendered page structure/copy/CTA behavior directly against Colorado's live code. `npm run typecheck`/`npm run lint`/`npm run build` all pass; all 12 approved routes plus 12 guide detail routes return `200` from a local dev server with the expected hero copy, nav order, and `state=KS`-tagged CTAs confirmed by direct inspection of the rendered HTML.
- **KS-19** (2026-09-06): Published the 12 Kansas source and document guides, using the same schema and page templates as `coloradoMarketing`'s `app/lib/explainers.ts`/`app/colorado/guides/*` (type + builder factory + flat entry array + index/detail page templates), simplified to drop Colorado's document-auto-matching machinery (regex/alias rules linking an imported document to a guide) since nothing in this repo indexes documents against guides. New `app/lib/explainers.ts` (12 entries: KCC/KGS agency roles, API numbers and KGS KIDs, C-1 intents, ACO-1 completions, T-1 transfers, CP-1/CP-4 plugging, lease-vs-well production, well statuses, PLSS locations, horizontal-well records, production freshness/revisions, and a closing data-limitations/verification checklist), `app/guides/page.tsx` (category-grouped index), `app/guides/[slug]/page.tsx` (detail template: at-a-glance meta list, important fields, free-form sections, what-it-proves/does-not-prove, related guides, official sources sidebar, FAQ, and a closing `state=KS`-tagged CTA into Future Wells Co). Every guide's content is grounded in facts already verified live against KGS/KCC in `kansasBackend` this session — real API-number structure (14-digit, Kansas prefix 15, KID as a documented-non-permanent fallback), the real lease-vs-well production distinction and sentinel-row handling from KS-10, the real multi-field well-status derivation and "unknown is surfaced, not guessed" rule from KS-04, and the horizontal-well-records guide's central example is KS-16's own real research finding (one of three KGS-flagged "horizontal" wells sampled turned out to be a 1928 scan with no directional content at all) — not Colorado's ECMC/DMR text with names substituted. Simplified `app/seo.ts`/`app/structured-data.tsx` (self-contained `seoMetadata`/`JsonLdBlock`/`BreadcrumbJsonLd`/`ArticleJsonLd`/`FaqPageJsonLd`/`WebSiteJsonLd`/`OrganizationJsonLd`, dropping Colorado's shared multi-file JSON-LD builder module and route-prefix allowlist this site has no equivalent of) support both KS-18's flat pages and these guide pages. `app/sitemap.xml/route.ts` now lists the 10 approved static pages plus every published guide slug (derived from `explainers.ts` directly, so a new guide is picked up automatically). Verified: all 12 guide slugs build via `generateStaticParams`, `npm run build` succeeds, and each guide's official-source citations, FAQ JSON-LD, and closing account CTA were confirmed present in rendered HTML output for at least one guide.
