# Production env for future-wells-ks-web.service. Copy alongside .env on
# the deploy host and fill in real values; never commit the filled-in copy.

NODE_ENV=production
PORT=3009
BACKEND_API_URL=http://127.0.0.1:4008
NEXT_PUBLIC_SITE_URL=https://futurewellskansas.com
NEXT_PUBLIC_INDEXING_ENABLED=false
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=
NEXT_PUBLIC_ANALYTICS_ENABLED=false
